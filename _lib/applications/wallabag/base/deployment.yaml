---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallabag
  namespace: wallabag
  labels:
    app.kubernetes.io/name: wallabag
    app.kubernetes.io/instance: wallabag-${ENVIRONMENT}
    app.kubernetes.io/version: "${WALLABAG_VERSION}"
    environment: "${ENVIRONMENT}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wallabag
  template:
    metadata:
      labels:
        app: wallabag
        node: worker
        env: "${ENVIRONMENT}"
        app.kubernetes.io/name: wallabag
        app.kubernetes.io/instance: wallabag-${ENVIRONMENT}
        app.kubernetes.io/version: "${WALLABAG_VERSION}"
    spec:
      securityContext:
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      # The wallabag/wallabag image is Alpine-based and chowns
      # /var/www/wallabag to nobody:nobody (UID 65534) at build time, so
      # running the main container as 65534 lets it write app/config/
      # parameters.yml, var/cache, var/logs, var/sessions, and web/assets/
      # images directly — no shadow mounts needed.
      #
      # The one path the image leaves root-owned is /etc/php81/conf.d/,
      # where the entrypoint templates 50_wallabag.ini from PHP_MEMORY_LIMIT.
      # This init container (root) copies that directory into an emptyDir
      # and chowns it to 65534 so the main container can write 50_wallabag.ini.
      - name: seed-php-conf
        image: wallabag/wallabag:${WALLABAG_VERSION}
        imagePullPolicy: Always
        securityContext:
          runAsUser: 0
          runAsNonRoot: false
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
            add:
            - CHOWN
            - DAC_OVERRIDE
            - FOWNER
          seccompProfile:
            type: RuntimeDefault
        command:
        - /bin/sh
        - -c
        - |
          set -eu
          cp -a /etc/php81/conf.d/. /seed/php-conf/
          chown -R 65534:65534 /seed/php-conf
        resources:
          requests:
            memory: 32Mi
            cpu: 50m
          limits:
            memory: 128Mi
            cpu: 500m
        volumeMounts:
        - name: wallabag-php-conf
          mountPath: /seed/php-conf
      containers:
      - name: wallabag-web
        image: wallabag/wallabag:${WALLABAG_VERSION}
        imagePullPolicy: Always
        # The wallabag entrypoint runs `su -c "composer install" nobody`
        # unconditionally (and for import/migrate modes). SUID-based privilege
        # switching doesn't work in this cluster — Talos's containerd applies
        # restrictions that prevent the SUID bit from elevating EUID even
        # with allowPrivilegeEscalation: true. We sidestep this by mounting
        # a passthrough `su` shim at /opt/shims/su (configmap wallabag-shims)
        # and prepending it to PATH below; the entrypoint resolves to the
        # shim instead of BusyBox su, no privilege change needed.
        securityContext:
          runAsNonRoot: true
          runAsUser: 65534
          runAsGroup: 65534
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
            add:
            - NET_BIND_SERVICE
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            memory: 256Mi
            cpu: 100m
          limits:
            memory: 1Gi
            cpu: 1000m
        ports:
        - containerPort: 80
        volumeMounts:
        - name: wallabag-php-conf
          mountPath: /etc/php81/conf.d
        - name: wallabag-shims
          mountPath: /opt/shims
          readOnly: true
        env:
        - name: PATH
          value: "/opt/shims:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
        - name: SYMFONY__ENV__DATABASE_USER
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: username
        - name: SYMFONY__ENV__DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: password
        - name: SYMFONY__ENV__DATABASE_NAME
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: database
        - name: SYMFONY__ENV__DATABASE_PORT
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: port
        - name: SYMFONY__ENV__DATABASE_HOST
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: server
        - name: SYMFONY__ENV__DATABASE_DRIVER
          value: "pdo_pgsql"
        - name: SYMFONY__ENV__FOSUSER_REGISTRATION
          value: "false"
        - name: POPULATE_DATABASE
          value: "true"
        - name: PHP_MEMORY_LIMIT
          value: 500M
        - name: SYMFONY__ENV__DOMAIN_NAME
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: url
        - name: SYMFONY__ENV__SECRET
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: app_secret
        - name: SYMFONY__ENV__REDIS_HOST
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: redis_host
        - name: SYMFONY__ENV__REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: wallabag-creds
              key: redis_password
      volumes:
      - name: wallabag-php-conf
        emptyDir: {}
      - name: wallabag-shims
        configMap:
          name: wallabag-shims
          defaultMode: 0755
          items:
          - key: su
            path: su
