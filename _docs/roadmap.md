# Roadmap & open items

!!! abstract "Source of truth"
    The authoritative, always-current open-items list is the **newest review** in [`reviews/`](reviews/home-0ps-review-2026-05-20.md), regenerated by the `/lab-review` command. This page is a curated summary of that tracker — when it drifts, trust the review.

_Summarised from: [reviews/home-0ps-review-2026-05-20.md](reviews/home-0ps-review-2026-05-20.md)._

## Suggested next sprint

1. **Resilience bundle for FreshRSS** — `ResourceQuota`/`LimitRange` (H-4) + `PodDisruptionBudget` (R-1) + `terminationGracePeriodSeconds` (R-7), one namespace, one PR.
2. **Homer polish** — flip `readOnlyRootFilesystem: true` once writable paths are enumerated (HM-1); fill in dashboard tiles (HM-2).
3. **Falco** (H-3) — enable; verify eBPF on Talos + ServiceMonitor scrape.
4. **K8s events → Loki** (O-4) — highest observability ROI; check Alloy RBAC first.
5. **Apply CoreDNS terraform** (TF-CoreDNS) — close the live-vs-IaC drift.
6. **Storage migration (S-tier)** — its own sprint: snapshot infra → reclaim fix → FreshRSS → Authentik → retire R2/S3.

## Open items by tier

=== "Security (HIGH)"

    | ID | Item | Where |
    | --- | --- | --- |
    | H-3 | Enable Falco | `_lib/controllers/kustomization.yaml`, `_lib/security/kustomization.yaml` |
    | H-4 | ResourceQuota + LimitRange per ns | per-app `base/` |
    | H-5 | Trivy operator | `_lib/security/trivy/` (empty) |

=== "Resilience (MEDIUM)"

    | ID | Item | Where |
    | --- | --- | --- |
    | R-1 | PodDisruptionBudgets | `_lib/applications/freshrss/base/` |
    | R-7 | terminationGracePeriodSeconds | `_lib/applications/freshrss/base/deployment.yaml` |
    | HM-1 | Homer read-only root FS | `_lib/applications/homer/base/deployment.yaml` |

=== "Observability"

    | ID | Item |
    | --- | --- |
    | O-4 | K8s Warning events → Loki (highest ROI) |
    | O-5 | Gateway hardening (headers, body limits, rate limiting) |
    | O-6 | Periodic posture scans (popeye/kubescape) |
    | O-9 | App-level dashboards/alerts |

=== "Storage (S-tier)"

    | ID | Item |
    | --- | --- |
    | S-1 | Snapshot infrastructure (blocks scheduled backups) |
    | S-2 | CNPG → single-instance static iSCSI zvol |
    | S-3 | Retire R2/S3 from CNPG path |
    | S-4 | iSCSI StorageClass reclaim → `Retain` |

    Full rationale: [ADR-0003](decisions/0003-cnpg-local-snapshots.md).

=== "Terraform / manual"

    | ID | Item |
    | --- | --- |
    | TF-CoreDNS | Apply CoreDNS split-horizon inlineManifest (committed, unverified) |
    | TF-wallabag | Destroy dead wallabag S3 stack |
    | — | Confirm `dev-cryptpad-data-pvc` TrueNAS zvol destroyed |
    | — | Beelink S13 BIOS power-loss = "Power On" (verify 6 nodes) |

## How this becomes live

Same approaches as [Status](status.md#making-this-live): a build-time step (mkdocs-macros) could parse the newest `reviews/*.md` and render this page automatically, so the roadmap never drifts from the tracker.
